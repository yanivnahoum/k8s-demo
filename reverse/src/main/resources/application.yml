server:
  port: 8090

management:
  endpoints:
    web:
      exposure:
        include: health

logging:
  level:
    root: info
    org.springframework.web.filter.CommonsRequestLoggingFilter: debug
